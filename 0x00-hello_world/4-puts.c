#include <stdio.h>

/**
 * here we want to print a line
 * we will be using the puts
 * we should return 0 always
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
